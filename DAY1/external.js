console.log("Hello there");
// JavaScript is a programming language used to make web pages interactive. 
// It is a versatile, high-level programming language that serves as one of the core technologies of the world wide web, alongside HTML and CSS. 
// It is case-sensitive and it is dynamically typed (variable types are determined at runtime). 
// It executes one task at a time (single-threaded).